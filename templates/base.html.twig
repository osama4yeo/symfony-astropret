<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Astropret - Location de Matériel{% endblock %}</title> {# Titre par défaut actualisé #}
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

        {% block stylesheets %}
            {# Bootstrap (exemple, si tu veux un style rapide) - tu peux le remplacer ou l'ajouter à tes propres styles #}
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">

            {# Les feuilles de style externes pour FullCalendar (déplace-les si pas besoin sur toutes les pages) #}
            <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.15/main.min.css" />
            {# <link href="https://cdn.jsdelivr.net/npm/@fullcalendar/core@6.1.15/main.min.css" rel="stylesheet" /> #}
            {# <link href="https://cdn.jsdelivr.net/npm/@fullcalendar/daygrid@6.1.15/main.min.css" rel="stylesheet" /> #}

            <link rel="stylesheet" href="{{ asset('assets/css/style.css') }}">
        {% endblock %}

        {# J'ai déplacé le bloc javascripts ici, c'est plus conventionnel de le mettre à la fin du body pour le chargement, mais le tien était dans head #}
        {# Tu peux le remettre dans <head> si tu as une raison spécifique, mais pour le moment, je le laisse ici pour la structure #}
    </head>
    <body>
        {# === L'EN-TÊTE EST MAINTENANT ICI, EN DEHORS DE TOUT BLOC SURCHARGEABLE === #}
        <header>
            <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
                <div class="container-fluid">
                    <a class="navbar-brand" href="{{ path('app_home') }}">Astropret</a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarNav">
                        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_home') }}">Accueil</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('calendar_view') }}">Calendrier</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('user_trombinoscope') }}">Trombinoscope</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_materiel_index') }}">Matériel</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_location_dispatch') }}">Location</a>
                            </li>
                            {# Correction du li vide et ajustement du lien profil #}
                            {% if app.user %} {# Le lien Profil n'a de sens que si l'utilisateur est connecté #}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('user_profile') }}">Profil</a>
                            </li>
                            {% endif %}
                        </ul>
                        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                            {% if app.user %}
                                {% if is_granted('ROLE_ADMIN') %}
                                    <li class="nav-item">
                                        <a class="nav-link" href="{{ path('reservation_index') }}">Gestion Prêts (Admin)</a>
                                    </li>
                                {% endif %}
                                <li class="nav-item"><span class="navbar-text me-2">Bonjour, {{ app.user.prenom }} {{ app.user.nom }}!</span></li>
                                <li class="nav-item"><a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a></li>
                            {% else %}
                                <li class="nav-item"><a class="nav-link" href="{{ path('app_register') }}">Inscription</a></li>
                                <li class="nav-item"><a class="nav-link" href="{{ path('app_login') }}">Connexion</a></li>
                            {% endif %}
                        </ul>
                    </div>
                </div>
            </nav>
        </header>
        {# === FIN DE L'EN-TÊTE === #}

        <main role="main" class="container mt-4">
            {% include 'partials/_flash_messages.html.twig' ignore missing %}

            {# Le div#calendar était ici, je le déplace dans le bloc body_content pour plus de flexibilité #}
            {# Si toutes tes pages ont le calendrier, tu peux le laisser en dehors du bloc. #}
            {# Sinon, chaque page qui a besoin du calendrier l'ajoutera dans son propre bloc body_content. #}

            {# J'ai renommé ton bloc body en body_content pour plus de clarté #}
            {% block body_content %}
                {# Si la page par défaut doit afficher le calendrier : #}
                {# <div id="calendar"></div> #}
            {% endblock %}
        </main>

        <footer class="footer mt-auto py-3 bg-light">
            <div class="container text-center">
                <span class="text-muted">© {{ "now"|date("Y") }} Astropret. Tous droits réservés.</span>
            </div>
        </footer>

        {% block javascripts_base %}
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
        {% endblock %}

        {# J'ai déplacé ton ancien bloc javascripts ici et l'ai renommé #}
        {% block javascripts_page_specific %}
            {# Les scripts JavaScript pour FullCalendar (à déplacer dans le template enfant si besoin spécifique) #}
            {# <script src="https://cdn.jsdelivr.net/npm/@fullcalendar/core@6.1.15/main.min.js"></script> #}
            {# <script src="https://cdn.jsdelivr.net/npm/@fullcalendar/daygrid@6.1.15/main.min.js"></script> #}
            {# <script src="https://cdn.jsdelivr.net/npm/@fullcalendar/core@6.1.15/locales/fr.js"></script> #}
            <script src="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.15/index.global.min.js"></script>
            {# Votre script local pour le calendrier (à charger uniquement sur la page du calendrier) #}
            {# <script src="{{ asset('assets/js/calendar.js') }}" defer></script> #}
        {% endblock %}
    </body>
</html>