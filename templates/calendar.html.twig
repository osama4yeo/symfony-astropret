{% extends 'base.html.twig' %}

{% block title %}Calendrier des événements{% endblock %}

{% block body_content %}
    <div class="container mt-4">
        <h2>Calendrier des événements</h2>

        {# 🔐 Affichage conditionnel pour les admins #}
        {% if is_granted('ROLE_ADMIN') %}
            <div class="text-end mb-3">
                <a href="{{ path('calendar_manage') }}" class="btn btn-primary">
                    Accéder à la gestion des événements
                </a>
            </div>
        {% else %}
            <div class="alert alert-warning text-center">
                Vous n'avez pas accès à la gestion des événements.
            </div>
        {% endif %}

        <div id="calendar"></div>
    </div>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link href="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.15/main.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }

        h2 {
            font-weight: 600;
            color: #343a40;
            text-align: center;
            margin-bottom: 30px;
        }
        
        #calendar {
            max-width: 100%;
            width: 100%;
            min-height: 700px;
            background-color: #ffffff;
            border-radius: 12px;
            padding: 20px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            overflow-x: auto;
        }

        /* Styles des événements */
        .fc-daygrid-event {
            background-color: #0b5ed7 !important;
            color: white !important;
            border: none !important;
            border-radius: 6px;
            padding: 2px 6px;
            font-size: 0.8rem;
            font-weight: 500;
            white-space: normal !important;
            overflow-wrap: break-word;
        }

        .fc-daygrid-event .fc-event-title,
        .fc-daygrid-event .fc-event-time {
            color: white !important;
        }

        /* Style du titre du calendrier (ex: Juin 2024) */
        .fc-toolbar-title {
            font-size: 1.4rem;
            font-weight: 500;
            color: #343a40;
            text-transform: capitalize; /* MODIFIÉ de uppercase à capitalize */
        }

        /* Styles des boutons FullCalendar */
        .fc-button {
            background-color: #0d6efd !important;
            border: none !important;
            border-radius: 6px !important;
            font-size: 0.9rem;
        }

        .fc-button-primary:not(:disabled):hover {
            background-color: #0b5ed7 !important;
        }

        /* Style des en-têtes de jours (Lundi, Mardi, ...) */
        .fc-col-header-cell-cushion { 
            color: black !important;
            text-decoration: none !important;
        }

        /* Styles pour les numéros de jour en noir et sans soulignement (GLOBAL) - AJOUTÉ */
        .fc-daygrid-day-number {
            color: black !important;
            text-decoration: none !important;
        }
        /* Pour les vues semaine/jour si besoin - AJOUTÉ */
        .fc-timegrid-axis-cushion,
        .fc-timegrid-slot-label {
            color: black !important;
            text-decoration: none !important;
        }
        /* S'assurer que les liens à l'intérieur des jours sont aussi stylés - AJOUTÉ */
        .fc-daygrid-day-frame a { 
            color: black !important;
            text-decoration: none !important;
        }


        .alert-warning {
            max-width: 600px;
            margin: auto;
            padding: 10px 20px;
            font-size: 0.95rem;
        }

        /* --- SECTION RESPONSIVE --- */
        @media (max-width: 768px) {
            #calendar {
                padding: 10px;
            }

            .fc-toolbar.fc-header-toolbar {
                flex-direction: column;
                align-items: center;
            }

            .fc-toolbar-chunk {
                margin-bottom: 8px;
                text-align: center;
            }

            .fc-toolbar-title {
                font-size: 1.2rem; 
                /* text-transform: capitalize; est hérité de la règle générale.
                   Si tu voulais ex: TOUT_MAJUSCULE sur mobile, tu ajouterais text-transform: uppercase; ici.
                   Le text-transform: uppercase; qui était ici a été retiré car il était redondant ou conflictuel avec le capitalize général.
                */
            }

            .fc-button {
                font-size: 0.8rem;
                padding: 4px 8px;
            }

            .fc-daygrid-event {
                font-size: 0.75rem;
                padding: 2px 4px;
            }

            .fc-col-header-cell-cushion { /* Cible le contenu de l'en-tête des jours */
                font-size: 0.7em !important; /* Réduit la taille de la police. Ajuste la valeur. */
                padding: 2px 1px !important; /* Réduit le padding interne. Ajuste. */
                line-height: 1.2; 
            }

            /* Optionnel: Si les jours sont encore trop serrés AVANT que le JS ne change le format */
            .fc-theme-standard .fc-scrollgrid th,
            .fc-theme-standard .fc-col-header-cell {
                padding-left: 1px !important;
                padding-right: 1px !important;
            }

            /* Les styles pour .fc-daygrid-day-number sont maintenant globaux et s'appliquent aussi ici. */
            /* Si tu avais besoin d'un style *différent* pour les numéros sur mobile, tu le mettrais ici. */
        }
    </style>
{% endblock %}

{% block javascripts_page_specific %}
    <script src="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.15/index.global.min.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const calendarEl = document.getElementById('calendar');
            // Fonction pour déterminer le format des en-têtes de jour
            function getDayHeaderFormat() {
                if (window.innerWidth < 576) { // Breakpoint où tu veux passer aux abréviations
                    return { weekday: 'short' }; // 'short' (Lun) ou 'narrow' (L)
                } else {
                    return { weekday: 'long' };  // 'Lundi'
                }
            }

            const calendar = new FullCalendar.Calendar(calendarEl, {
                initialView: 'dayGridMonth',
                locale: 'fr',
                firstDay: 1,
                dayHeaderFormat: getDayHeaderFormat(),
                dayHeaderFormat: { weekday: 'long' },
                events: "{{ path('calendar_events') }}",

                // ✅ UNE SEULE FOIS - barre de navigation complète
                headerToolbar: {
                    left: 'prev,next today',
                    center: 'title',
                    right: 'dayGridMonth,timeGridWeek,timeGridDay,listMonth'
                },

                // ✅ Traduction des boutons
                buttonText: {
                    today:    'Aujourd\'hui',
                    month:    'Mois',
                    week:     'Semaine',
                    day:      'Jour',
                    list:     'Liste'
                },

                // ✅ Définition des vues (facultatif mais propre)
                views: {
                    dayGridMonth: { buttonText: 'Mois' },
                    timeGridWeek: { buttonText: 'Semaine' },
                    timeGridDay: { buttonText: 'Jour' },
                    listMonth:    { buttonText: 'Liste' }
                },

                // ✅ Affichage personnalisé du contenu d’un événement
                eventContent: function(arg) {
                    return {
                        html: '<div class="fc-event-title" style="white-space: normal;">' + arg.event.title + '</div>'
                    };
                },

                // ✅ Tooltip au survol
                eventDidMount: function(info) {
                    const tooltip = document.createElement('div');
                    tooltip.classList.add('fc-event-tooltip');
                    tooltip.style.position = 'absolute';
                    tooltip.style.zIndex = '1000';
                    tooltip.style.background = 'rgba(0, 0, 0, 0.75)';
                    tooltip.style.color = '#fff';
                    tooltip.style.padding = '5px 10px';
                    tooltip.style.borderRadius = '6px';
                    tooltip.style.fontSize = '0.8rem';
                    tooltip.style.display = 'none';

                    tooltip.innerText = info.event.title + (info.event.extendedProps.description ? '\n' + info.event.extendedProps.description : '');
                    document.body.appendChild(tooltip);

                    info.el.addEventListener('mouseenter', function(e) {
                        tooltip.style.display = 'block';
                        tooltip.style.left = e.pageX + 'px';
                        tooltip.style.top = e.pageY + 'px';
                    });

                    info.el.addEventListener('mousemove', function(e) {
                        tooltip.style.left = e.pageX + 10 + 'px';
                        tooltip.style.top = e.pageY + 10 + 'px';
                    });

                    info.el.addEventListener('mouseleave', function () {
                        tooltip.style.display = 'none';
                    });
                }
            });

            calendar.render();
        });
    </script>
{% endblock %}
